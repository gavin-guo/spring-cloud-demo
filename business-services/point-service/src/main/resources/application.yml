liquibase:
  change-log: classpath:db/changelog/changelog.xml
  default-schema: SCHEMA_POINT
  drop-first: true
  enabled: true

---
spring:
  profiles: development

server:
  port: 9020

---
spring:
  profiles: test