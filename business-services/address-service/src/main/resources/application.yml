liquibase:
  change-log: classpath:db/changelog/changelog.xml
  default-schema: SCHEMA_ADDRESS
  drop-first: true
  enabled: true

---
spring:
  profiles: development

server:
  port: 9030

---
spring:
  profiles: test