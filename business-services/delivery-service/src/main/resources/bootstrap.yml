spring:
  application:
    name: delivery-service

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/,http://${EUREKA_HOST:localhost}:8762/eureka/
  instance:
    prefer-ip-address: true

encrypt:
  fail-on-error: false

---
spring:
  profiles: development
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: development
      label: master
      username: gavin
      password: gavin
      allow-override: true

---
spring:
  profiles: test
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: test
      label: master
      username: gavin
      password: gavin
      allow-override: true