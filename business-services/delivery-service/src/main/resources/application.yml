liquibase:
  change-log: classpath:db/changelog/changelog.xml
  default-schema: SCHEMA_DELIVERY
  drop-first: false
  enabled: true

---
spring:
  profiles: development

server:
  port: ${random.int[9070,9079]}

---
spring:
  profiles: test