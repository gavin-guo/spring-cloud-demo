liquibase:
  change-log: classpath:db/changelog/changelog.xml
  default-schema: SCHEMA_PRODUCT
  drop-first: true
  enabled: false

---
spring:
  profiles: development

server:
  port: 9040

---
spring:
  profiles: test